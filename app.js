"use strict";

const $=id=>document.getElementById(id);
const authView=$("authView"),mainView=$("mainView"),authForm=$("authForm"),authSubmit=$("authSubmit"),authError=$("authError"),emailInput=$("email"),passwordInput=$("password"),clipboardList=$("clipboardList"),emptyState=$("emptyState"),settingsPanel=$("settingsPanel"),editorModal=$("editorModal"),editorText=$("editorText"),editorError=$("editorError"),toast=$("toast");
let auth=null,db=null,unsubscribeClips=null,unsubscribeDevices=null,authMode="login",editingId=null,currentDeviceId=localStorage.getItem("cm_device_id")||crypto.randomUUID();
localStorage.setItem("cm_device_id",currentDeviceId);

const deviceType=()=>{const w=screen.width,h=screen.height,ua=navigator.userAgent.toLowerCase();if(/ipad|tablet|android(?!.*mobile)/.test(ua)||(Math.min(w,h)>=600&&Math.min(w,h)<1000))return"tablet";if(/mobile|iphone|ipod|android/.test(ua)||Math.min(w,h)<600)return"phone";return"pc"};
const deviceLabel=()=>{const ua=navigator.userAgent; if(/iphone/i.test(ua))return"iPhone";if(/ipad/i.test(ua))return"iPad";if(/android/i.test(ua))return deviceType()==="tablet"?"Android タブレット":"Android スマートフォン";if(/windows/i.test(ua))return"Windows PC";if(/macintosh/i.test(ua))return"Mac";if(/linux/i.test(ua))return"Linux PC";return"ブラウザ端末"};
function iconFor(type){return type==="pc"?'<svg viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="12" rx="1"/><path d="M8 20h8M12 16v4"/></svg>':type==="tablet"?'<svg viewBox="0 0 24 24"><rect x="5" y="2.5" width="14" height="19" rx="2"/><path d="M10 18.5h4"/></svg>':'<svg viewBox="0 0 24 24"><rect x="7" y="2.5" width="10" height="19" rx="2"/><path d="M10.5 18.5h3"/></svg>'}
function fmt(ts){if(!ts)return"-";const d=ts.toDate?ts.toDate():new Date(ts);return new Intl.DateTimeFormat("ja-JP",{year:"numeric",month:"numeric",day:"numeric",hour:"2-digit",minute:"2-digit"}).format(d)}
function ageText(ts){if(!ts)return"";const ms=(ts.toDate?ts.toDate():new Date(ts)).getTime()+5*86400000-Date.now();if(ms<=0)return"まもなく削除";const d=Math.floor(ms/86400000),h=Math.floor(ms/3600000)%24;return d?`あと${d}日`:`あと${h}時間`}

function init(){
  if(!window.CM_FIREBASE_CONFIGURED){showSetup();return}
  firebase.initializeApp(firebaseConfig);auth=firebase.auth();db=firebase.firestore();
  auth.onAuthStateChanged(user=>user?enterApp(user):enterAuth());
  setupUI();
}
function setupUI(){
  document.querySelectorAll(".tab").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".tab").forEach(x=>x.classList.remove("active"));b.classList.add("active");authMode=b.dataset.mode;authSubmit.textContent=authMode==="login"?"ログイン":"アカウントを作成";authError.textContent=""}));
  authForm.addEventListener("submit",async e=>{e.preventDefault();authError.textContent="";authSubmit.disabled=true;try{if(authMode==="login")await auth.signInWithEmailAndPassword(emailInput.value.trim(),passwordInput.value);else await auth.createUserWithEmailAndPassword(emailInput.value.trim(),passwordInput.value)}catch(err){authError.textContent=authErrorText(err)}finally{authSubmit.disabled=false}});
  $("settingsButton").addEventListener("click",openSettings);$("closeSettings").addEventListener("click",closeSettings);$("settingsBackdrop").addEventListener("click",closeSettings);$("logoutButton").addEventListener("click",()=>auth.signOut());
  $("newClipboard").addEventListener("click",()=>openEditor());$("closeEditor").addEventListener("click",closeEditor);$("cancelEditor").addEventListener("click",closeEditor);$("editorBackdrop").addEventListener("click",closeEditor);$("saveEditor").addEventListener("click",saveClipboard);
}
function authErrorText(e){const map={"auth/invalid-email":"メールアドレスが正しくありません。","auth/user-not-found":"アカウントが見つかりません。","auth/wrong-password":"パスワードが違います。","auth/email-already-in-use":"このメールアドレスはすでに登録されています。","auth/weak-password":"パスワードは6文字以上にしてください。","auth/too-many-requests":"試行回数が多すぎます。少し待ってください。"};return map[e.code]||"処理に失敗しました。入力内容とFirebase設定を確認してください。"}
function showSetup(){authView.classList.remove("hidden");mainView.classList.add("hidden");authError.textContent="Firebase設定がまだありません。firebase-config.js にWebアプリの設定値を入れてください。";authSubmit.disabled=true}
function enterAuth(){authView.classList.remove("hidden");mainView.classList.add("hidden");if(unsubscribeClips)unsubscribeClips();if(unsubscribeDevices)unsubscribeDevices()}
async function enterApp(user){authView.classList.add("hidden");mainView.classList.remove("hidden");$("accountEmail").textContent=user.email||"-";await registerDevice(user);subscribeClipboard(user);subscribeDevices(user)}
async function registerDevice(user){const ref=db.collection("users").doc(user.uid).collection("devices").doc(currentDeviceId);await ref.set({type:deviceType(),name:deviceLabel(),userAgent:navigator.userAgent,lastSeen:firebase.firestore.FieldValue.serverTimestamp(),createdAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true})}
function subscribeDevices(user){if(unsubscribeDevices)unsubscribeDevices();unsubscribeDevices=db.collection("users").doc(user.uid).collection("devices").orderBy("lastSeen","desc").onSnapshot(snap=>{$("deviceList").innerHTML=snap.docs.map(d=>{const x=d.data();return `<div class="device"><div class="device-icon">${iconFor(x.type)}</div><div class="device-info"><div class="device-name">${esc(x.name||"不明な端末")}</div><div class="device-meta">最終利用 ${fmt(x.lastSeen)}</div></div>${d.id===currentDeviceId?'<span class="device-current">この端末</span>':''}</div>`}).join("")||'<p class="section-note">登録端末はありません。</p>'},()=>{$("deviceList").innerHTML='<p class="section-note">端末情報を読み込めませんでした。</p>'})}
function subscribeClipboard(user){if(unsubscribeClips)unsubscribeClips();unsubscribeClips=db.collection("users").doc(user.uid).collection("clipboard").orderBy("updatedAt","desc").onSnapshot(snap=>{clipboardList.innerHTML="";snap.docs.forEach(d=>renderCard(d.id,d.data()));emptyState.classList.toggle("hidden",!snap.empty)})}
function renderCard(id,x){const card=document.createElement("article");card.className="clipboard-card";const text=x.text||"";const long=text.length>240||text.split("\n").length>6;card.innerHTML=`<div class="card-meta"><span>${esc(x.deviceName||"端末")}</span><span class="card-date">作成 ${fmt(x.createdAt)} ・ 更新 ${fmt(x.updatedAt)}</span></div><div class="card-text ${long?"collapsed":""}" data-id="${id}" title="タップして編集">${esc(text)}</div><div class="card-footer"><div class="card-actions"><button class="mini-button copy" data-id="${id}">コピー</button><button class="mini-button share" data-id="${id}">共有</button></div>${long?'<button class="expand-button">全文を表示</button>':''}</div>`;card.querySelector(".card-text").addEventListener("click",()=>openEditor(id,x));card.querySelector(".copy").addEventListener("click",()=>copyText(text));card.querySelector(".share").addEventListener("click",()=>shareText(text));const exp=card.querySelector(".expand-button");if(exp)exp.addEventListener("click",()=>{const t=card.querySelector(".card-text");t.classList.toggle("collapsed");exp.textContent=t.classList.contains("collapsed")?"全文を表示":"折りたたむ"});clipboardList.appendChild(card)}
function esc(v){return String(v).replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","\\":"&#92;"}[c]||c))}
function openEditor(id=null,data=null){editingId=id;$("editorTitle").textContent=id?"クリップボードを編集":"クリップボードを追加";editorText.value=data?.text||"";editorError.textContent="";editorModal.classList.remove("hidden");setTimeout(()=>editorText.focus(),50)}
function closeEditor(){editorModal.classList.add("hidden");editingId=null}
async function saveClipboard(){const text=editorText.value;if(!text.trim()){editorError.textContent="テキストを入力してください。";return}const user=auth.currentUser;const base={text,deviceId:currentDeviceId,deviceName:deviceLabel(),updatedAt:firebase.firestore.FieldValue.serverTimestamp()};try{if(editingId)await db.collection("users").doc(user.uid).collection("clipboard").doc(editingId).update(base);else await db.collection("users").doc(user.uid).collection("clipboard").add({...base,createdAt:firebase.firestore.FieldValue.serverTimestamp(),expiresAt:firebase.firestore.Timestamp.fromMillis(Date.now()+5*86400000)});closeEditor();showToast(editingId?"更新しました":"追加しました")}catch(e){editorError.textContent="保存できませんでした。Firestoreの設定を確認してください。"}}
async function copyText(text){try{await navigator.clipboard.writeText(text);showToast("コピーしました")}catch{showToast("コピーできませんでした")}}
async function shareText(text){if(!navigator.share){await copyText(text);return}try{await navigator.share({text})}catch(e){if(e.name!=="AbortError")showToast("共有できませんでした")}}
function openSettings(){settingsPanel.classList.remove("hidden");settingsPanel.setAttribute("aria-hidden","false")};function closeSettings(){settingsPanel.classList.add("hidden");settingsPanel.setAttribute("aria-hidden","true")};let toastTimer;function showToast(t){toast.textContent=t;toast.classList.add("show");clearTimeout(toastTimer);toastTimer=setTimeout(()=>toast.classList.remove("show"),1800)}
window.addEventListener("load",init);
